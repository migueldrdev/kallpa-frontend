import { enableDebugTools } from "@angular/platform-browser";

export const environment = {
  production: false,
  apiUrl: 'http://localhost:8081/api/v1',
  apiTimeout: 3000, // 30 seconds
  enableDebugLog: true
};
